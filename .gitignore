node_modules/
dist/
.cache/
*.db
*.db-wal
*.db-shm

# Working artifacts, not repo history.
plan/

# Real captures are megabytes each and are not checked into the repository.
test/fixtures/real/

public/app.css

# The project skills are part of the repo, whatever a global ignore says.
!.claude/
!.claude/**

# Rendered pages for a visual review. Rebuild them, do not commit them.
preview/
